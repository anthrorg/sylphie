# 2026-03-30 -- E9-T014: WebConfig NestJS ConfigModule Integration

## Changes
- NEW: `src/web/web.config.ts` -- WebConfig factory using registerAs('web') pattern mapping 15 WEB_* environment variables to 7 typed configuration sections (HttpConfig, CorsConfig, WebSocketConfig, GraphVisualizationConfig, TelemetryConfig, HealthCheckConfig, DevelopmentConfig)
- MODIFIED: `src/shared/shared.module.ts` -- Added webConfig import and registered it in ConfigModule.forRoot load array alongside appConfig
- MODIFIED: `.env.example` -- Added all 15 WEB_* variables with descriptive comments and sensible defaults

## Wiring Changes
- WebConfig now registered globally via SharedModule, accessible to all subsystems via `ConfigService.get<WebConfig>('web')`
- No new module dependencies; reuses existing @nestjs/config pattern

## Known Issues
- Pre-existing TypeScript error in `src/web/interfaces/web.interfaces.ts` (ws module type definition) does not affect new code
- Type check passes for modified files without errors

## Gotchas for Next Session
- Services consuming WebConfig should follow the same pattern as AppConfig consumers (inject ConfigService, call get<WebConfig>('web'))
- All boolean env vars use strict string comparison ('true' === 'true') to avoid truthy coercion bugs
- Numeric env vars use parseInt/parseFloat with radix 10 for consistency with existing code
