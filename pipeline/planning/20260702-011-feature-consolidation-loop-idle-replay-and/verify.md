# Opus plan-verify verdict — 20260702-011

verdict: **needs-replan**

## Summary
I verified the plan's load-bearing claims directly against source and they hold: episodic ring buffer is 50 slots (episodic-memory.service.ts:52); packages/learning ALREADY has a done CognitiveAwareness-triggered Insight-synthesis pipeline (learning.service.ts:263-267 subscribes push-style to driveState$ and calls forceCycle above threshold 0.5; cross-session-synthesis.service.ts:181/441/468 matches :Insight-[:REVEALS]->:Entity pairs and CREATEs new :Insight nodes with SYNTHESIZES edges; conversation-reflection.service.ts:481 also CREATEs :Insight). So the source's central premise ("nothing turns verified atoms into connected understanding") is genuinely false, and this item would collide with the existing pipeline on both the CognitiveAwareness trigger and the :Insight label with an incompatible shape. That is a real architectural fork (Q1), not a nitpick — the planner correctly surfaced it to architect rather than guessing. Routing to replan is right, and it is doubly warranted: the item is also the 4th link in a fully unbuilt dependency chain (siblings 008 contradiction-scanner fix, 015 migration framework, 018 theory verdicts) — none have contract tickets yet, so the epic cannot reach queue regardless of how Q1 resolves. Ticket coverage of the source is otherwise strong and every acceptance criterion carries a genuinely runnable check (grep, migration idempotency, yarn test, Cypher provenance queries, drive-trace assertions); CANON is respected throughout (0.60 pre-guardian ceiling asserted in -c, push-only drive isolation asserted in -b, additive-only migration.md). Do not build any of this until architect rules Q1 and the three sibling items land as contract work.

## Critical gaps
- Triple dependency block: this item's tickets depend on sibling pipeline items 008 (fixes the no-op contradiction scanner ticket -d's AC needs), 015 (migration framework ticket -a's schema write must ride), and 018 (produces the theory verdicts tickets -b/-c consume) — NONE of which exist as contract tickets yet. The epic cannot enter queue until they land; whoever replans must sequence 008/015/018 first.
- Minor coverage gap: source's process step 're-weight confidence across related nodes' has no ticket that explicitly owns the re-weighting action — it is only implied via the MERGE-raises-only audit in -d (framed around contradiction pruning) and the EWC regression check in -e. Confirm during replan whether a dedicated re-weighting behavior is intended or whether it is subsumed by the existing MERGE-raises-only path.
- Minor atomicity: ticket -a bundles an architect decision-gate + a migration script (blocked on item 015 existing) + a shape unit test into one 'prototype' ticket; the migration-script criterion cannot complete until 015's framework lands, so -a is partially unbuildable on its own. Acceptable for prototype but flag when it reaches refine.

## Architect questions
- Q1 (blocks tickets -a and -b): Should decision-making's new episodic/theory consolidation loop (a) feed candidates into the ALREADY-BUILT, done EP-12 CognitiveAwareness pipeline in packages/learning — extending cross-session-synthesis's candidate sources and reusing the existing :Insight shape + InsightType enum + pre-guardian confidence cap — or (b) use a distinct WKG node label (e.g. :ConsolidationInsight) with its own trigger explicitly coordinated against the existing driveState$ subscriber so the two don't race on the same CognitiveAwareness pressure and don't leave :Insight with two incompatible property shapes? This determines package ownership of the write path, whether atlas extends or defines schema (ticket -a), and whether the drive trigger is 'add a second coordinated subscriber' or 'extend the existing one' (ticket -b). Verified live: both subsystems already fire on CognitiveAwareness and write :Insight, so the collision is concrete, not hypothetical.
