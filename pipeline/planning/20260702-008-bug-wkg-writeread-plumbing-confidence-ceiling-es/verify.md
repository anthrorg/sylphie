# Opus plan-verify verdict — 20260702-008

verdict: **needs-rework**

## Summary
Strong plan — I re-verified every load-bearing claim by reading the actual code, and the planner's diagnoses are accurate, including the non-obvious calls: the RELIEVES bullet is correctly REFUTED (wkg-bootstrap.service.ts:112-134 does MERGE 12 :Drive nodes with drive_name at OnModuleInit, so the :900 MATCH matches), the contradiction scanner is a deeper no-op than source stated (detect-contradictions writes CONTRADICTS on the EDGE between entity endpoints, never on a procedure node — traversal fix is right), and the matchProcedures coalesce mirrors the existing action-retriever precedent. Tickets 008-a/b/c/d are atomic, CANON-compliant (008-a + the dry-run/--confirm/backup-first migration RESTORE the 0.60 ceiling), and every AC has a genuinely runnable check — they are queue-ready. BLOCKER: ticket 008-e's AC to change bootstrap seed confidence 0.60→0.40 would build a functional REGRESSION. action-retriever's primary candidate query (action-retriever.service.ts:321-333) filters WHERE p.confidence >= CONFIDENCE_THRESHOLDS.retrieval (0.50), so seeds stamped at 0.40 would be excluded from retrieval entirely and every bootstrap procedure (research_entity, etc.) would silently vanish from the candidate path. The planner scoped this as trivial value-alignment hygiene, but 0.40-vs-0.50 is a design fork (is the 0.60 seed value the bug, or the 0.40 doc?) that needs an architect micro-ruling. Fix is small: carve that one AC out of 008-e into an open_question for architect and let 008-a/b/c/d + the other six 008-e ACs proceed — hence needs-rework, not a full replan. Two non-blocking notes: 008-a and 008-b both edit wkg-context.service.ts (dedup branch vs writeEntity/writeRelationship/INVOLVES-RELIEVES loop) so flag parallel-build merge coordination; and 008-c must confirm the scanner caller actually passes node_id as procedureId. All source concerns are otherwise covered; no scope creep (the migration script was anticipated by source).

## Critical gaps
- Ticket 008-e AC 'stamps BASE_CONFIDENCE = 0.40' would suppress ALL bootstrap seed procedures: action-retriever.service.ts:321-333 fetches candidates WHERE p.confidence >= CONFIDENCE_THRESHOLDS.retrieval (0.50), and 0.40 < 0.50, so seeds become un-retrievable via the primary path. Planner mis-scoped a design fork as trivial hygiene; as written a builder would ship a regression.
- Non-blocking: 008-a (writeActionProcedure dedup branch ~795-846) and 008-b (writeEntity/writeRelationship 364-471 + INVOLVES/RELIEVES loop 890-903) both edit wkg-context.service.ts; both P1, depends_on=[] — flag same-file merge coordination if built in parallel worktrees.
- Non-blocking: 008-c matches the procedure on node_id — builder must confirm ContradictionScannerService.scan()'s caller passes the node's node_id (not a differently-shaped id) as procedureId, or the corrected query still matches nothing.

## Architect questions
- Bootstrap seed ActionProcedure confidence: provenance.types.ts documents SYSTEM_BOOTSTRAP base = 0.40, but action-retriever's candidate query filters confidence >= CONFIDENCE_THRESHOLDS.retrieval (0.50), so seeds stamped 0.40 would never be retrieved. Which is authoritative — (a) fix the doc and keep seeds at a retrievable value (>=0.50/0.60), (b) lower seeds to 0.40 AND change how SYSTEM_BOOTSTRAP seeds are treated at retrieval (exempt from the 0.50 floor), or (c) something else? This resolves ticket 008-e's seed-confidence AC.
