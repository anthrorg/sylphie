# Opus plan-verify verdict — 20260702-014

verdict: **needs-replan**

## Summary
Concur with the Sonnet planner's replan recommendation. The plan itself is high-quality and I independently verified every load-bearing claim against the actual code: TrainingSample (schemas.py:116-126) requires fused_embedding/episodic_context/modality_embeddings/response_embedding; event.types.ts has zero 'embedding' matches; ActionExecutedEvent (event.types.ts:567-576) persists only actionId/actionType/arbitrationType; training/replay.py is genuinely the unrelated EWC ring-buffer (docstring line 1); bootstrap thresholds are 0.85 partial / 0.70 demotion / 0.90 full (bootstrap.py:55-57), default shadow. OQ-1 is a real, verified architectural fork — the embeddings a TrainingSample needs are NOT persisted in any event payload, and schemas.py:103-104 explicitly states the input vector 'cannot be reconstructed' by the sidecar and is only optionally attached to responses. This is not a nitpick: it decides whether full-fidelity byte-identical replay (branch a) is even possible, or whether the tool must be rescoped to label-only reconstruction (branch b, which weakens AC2 determinism and AC3 audit-agreement), or whether it needs an event-schema field the source's own non-goals forbid (branch c). AC1/AC2/AC3 for tickets 014-b and 014-c cannot be finalized until architect rules the branch. Completeness is good (every source concern maps to a ticket), CANON is clean (push-only into /cognition/train, provenance field noted, retrained model re-enters shadow and re-earns the unmodified gate, DB read-only so migration.md n/a is correct), and non-goals correctly fence off scope creep. 014-a (the spike to resolve OQ-1) and the epic shape are build-ready as written; only 014-b/014-c's acceptance wording is contingent on the ruling. Two minor non-blocking wrinkles the architect/coordinator should note: 014-c conflates 'audit-stage agreement' with the 0.85 threshold that code labels the *partial* threshold (stage/threshold mapping needs pinning), and source AC4's manifest-comparison half is not ticketable yet because verified_training_samples lives in unplanned sibling item 016.</parameter>
<parameter name="architect_questions">["OQ-1 (blocking 014-b/014-c acceptance criteria): The TrainingSample inputs (fused_embedding[768], episodic_context[768], modality_embeddings, response_embedding) are verified absent from every event payload today. Which branch is authoritative? (a) They are deterministically recomputable offline from raw sensory/text inputs + drive-state-at-timestamp re-run through the same versioned fusion path — making replay a byte-identical reconstruction and AC1/AC2/AC3 hold as written; (b) They are NOT recomputable (raw inputs not retained at needed granularity, or fusion path not cheaply re-runnable offline) — rescoping replay to label-only reconstruction and weakening AC2 determinism + AC3 audit-agreement claims; or (c) Add the missing fields to the event payload — which the source's non-goals explicitly forbid and would require a separate item sequenced ahead. Note: schemas.py:103-104 states the assembled input vector 'cannot be reconstructed' by the sidecar and global_input_vector is only optionally attached to responses (not confirmed logged to the events table) — architect should check whether global_input_vector is actually persisted before affirming branch (a).", "014-c stage/threshold mapping: source AC3 asks for 'at least audit-stage agreement', but the plan's runnable check uses 0.85, which bootstrap.py:55 labels the partial-graduation threshold. Confirm the shadow->audit->partial->full stage-to-threshold mapping so 014-c's acceptance check targets the correct stage boundary."]

