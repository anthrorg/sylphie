# Opus plan-verify verdict — 20260702-015

verdict: **needs-replan**

## Summary
Plan is factually excellent — I independently spot-checked all six load-bearing claims against source and every one holds (ACTION_TYPE_DEFAULTS@rules.ts:80, zero `veto` hits in packages, infra/migrations/001+002 convention, ProvenanceMissingError being missing-only not malformed, WkgBootstrapService OnModuleInit anchor, package.json:37-44 hand-named-per-migration). No scope creep (it actually trims: drops the impossible "Prisma→Neo4j", excludes PKG, defers tensor). Every AC has a genuinely runnable check. BUT three tickets carry design forks that must not be guessed at build time, so the item is not queue-ready as a whole: (1) 015-f cannot satisfy source AC4 — its "veto logic" checksum target maps to NO code object anywhere; (2) 015-c AC3 is written as "pick one behavior during the build," which punts a PRODUCTION-DB-safety decision (auto-apply-on-boot vs refuse) to the build agent — exactly the ambiguity that must route with the question written, not guessed; (3) 015-e is hard-blocked on item 20260702-016 which is still unplanned, and 016 also underpins the source's manifest-stamp clause and invariant #1. Crucially, tickets 015-a, 015-b, and 015-d are fork-free and buildable NOW — recommend the coordinator split those off to queue immediately and route only c/e/f to architect (and plan 016 first). No CANON violation in the plan as written, but two concrete build-watch items ride along (see gaps).

## Critical gaps
- Source AC4 unsatisfiable as written: 015-f only covers the ACTION_TYPE_DEFAULTS half; the paired 'veto logic' checksum has no code artifact (zero grep hits). The epic cannot close AC4 until architect names the concrete object. Correctly deferred by the planner, but it means the item is not fully buildable-to-spec.
- 015-c AC3 defers a production-safety behavior (on a recognized-but-pending migration at boot: auto-apply via runner vs refuse-and-require-manual --confirm) to the build agent's discretion. Auto-apply risks an accidental live schema change on every deploy; this must be a ruling before queue, not a build-time coin-flip.
- 015-e is hard-blocked: the snapshot manifest/checksum format it verifies against does not exist in code or contract.yaml (item 20260702-016 still unplanned in pipeline/planning/). Source invariant #1 and the 'stamped in the snapshot manifest' half of the What-it-should-do also depend on 016. Sequence 016 before pulling 015-e.
- CANON build-watch (provenance): the :SchemaVersion singleton node 015-b writes into WKG/SKG/OKG must carry a provenance source or the ProvenanceMissingError guard (CANON §7, enforced on WKG writes) will reject it. The plan/migration.md must specify the provenance stamp or an exempt write path; not currently called out.
- CANON build-watch (drive isolation): 015-d's drive-state sanity check must read the drive's PERSISTED state store, not RPC into the drive-engine process, to respect drive isolation (Std-1 / no pull-read into-or-out-of the drive). The ticket says 'a drive-state row' which implies a store read — good — but the build must not implement it as a call into the drive engine.
- Minor atomicity: 015-d bundles three distinct invariant families (cross-store referential integrity, Neo4j schema conformance, drive-state sanity) under 5 ACs. Coherent as one boot invariant-pass harness, but could be split if the build proves heavy; not a blocker.

## Architect questions
- Floor-integrity (CANON Std-6, source AC4): what concrete, checksummable code object constitutes 'the veto logic' that pairs with ACTION_TYPE_DEFAULTS? Candidates: the confidence-ceiling (0.60-until-guardian-confirmed) enforcement gate, the guardian-asymmetry x2/x3 multiplier code, or an executor hard-stop/override path. 015-f is scoped to exclude it until named.
- 015-c pending-migration boot behavior: on a recognized-but-not-yet-applied migration at boot, should the system auto-apply it via the 015-a runner, or refuse and require an explicit manual --confirm apply? This is a production-deploy safety default (accidental live schema change vs boot deadlock in a fresh env) and should not be decided by the build agent.
- Sequencing: should pipeline item 20260702-016 (snapshot-restore) be planned and queued before 015-e is pulled off refine, and will the manifest format 016 produces expose a per-store-dump checksum field that 015-e's invariant #1 can verify against?
