# Opus plan-verify verdict — 20260702-009

verdict: **needs-replan**

## Summary
Verified every file:line claim in source.md and plan.md against main — all accurate (recallKeyForQuestion fact-only at deliberation-helpers.ts:518-530; inferGrounding's 4-branch cascade with no drive source at 616-631; GREETING/EMOTION→LLM_ASSISTED fallback at deliberation.service.ts:431-433; drive state injected but never GROUNDED at 347-368; groundedBy closed union 'OKG'|'WKG'|null at communication.types.ts:178; computeRecallRetrieval never skips the LLM at decision-making.service.ts:652). The bug is real and the plan is high quality. Completeness is good — every source concern maps to a ticket (009-a classifier, 009-b pure responder, 009-c wiring, 009-d frontend regression-proof) and the theater-prohibition concern is covered by 009-b's flat-state AC. ACs are genuinely runnable (named spec files + yarn jest commands). No CANON violation: drive isolation is read-only via IDriveStateReader, no DB surface (migration.md n/a is correct), provenance is the whole point of Fork 2. Route is needs-replan — NOT a planner gap. The planner correctly SURFACED two genuine architecture forks rather than guessing: (1) where the no-LLM short-circuit lives, and (2) how to type drive-snapshot provenance. 009-a and 009-b are unaffected by either fork and are genuinely build-ready now; only 009-c is blocked. Coordinator could split 009-a/009-b to queue while the architect rules.

## Critical gaps
- Metric-gate interaction the plan missed: decision-making.service.ts:662+ documents a knowledge_retrieval capability metric that gates its denominator on RESPONSE_GENERATED rows with payload.intent='QUESTION'; branches that skip the LLM monologue previously persisted intent=NULL and left the capability inert. A pre-arbitration self-state short-circuit (Fork 1 Option B) hits exactly this — 009-c must persist an intent (or explicitly handle the metric) or it silently breaks knowledge_retrieval telemetry. Add this to 009-c's ACs once the fork is ruled.
- No ticket specifies the confidence value the GROUNDED self-state response carries. The existing short-circuit returns 0.85 for GREETING/EMOTION (deliberation.service.ts:484); 009-b/009-c should define the emitted confidence explicitly. Minor (Std-3's 0.60 ceiling governs guardian-unconfirmed appraisal/learning confidence, not a deterministic readout of ground-truth state), but leave it unstated and the builder will guess.

## Architect questions
- Fork 1 — control-flow locus: build the no-LLM self-state short-circuit as (A) a TYPE_1 procedure/reflex through arbitration.service.ts's existing graduation machinery (reuses procedure infra but pulls self-state into the procedure system + TYPE_1 metrics/attractor-monitor), or (B) a pre-arbitration early-return in decision-making.service.ts that returns a complete CycleResponse before arbitration and bypasses all three deliberate() call sites (1090/1179/1291) for that cycle? Neither existing pattern is a drop-in: computeRecallRetrieval only grounds LLM-generated text post-hoc, it never skips the LLM (confirmed — even OKG-recall runs the monologue first). This is a genuinely new control-flow shape either way. RIDER: whichever option is chosen must persist intent='QUESTION' (or equivalent) on the short-circuited RESPONSE_GENERATED row so the knowledge_retrieval metric denominator at decision-making.service.ts:662+ is not silently zeroed.
- Fork 2 — provenance typing: record drive-snapshot provenance by (A) widening the closed groundedBy union to add a 'DRIVE' member (communication.types.ts:178 + RecallSource in recall-retrieval.ts + 5+ consumer sites in decision-making.service.ts/deliberation.service.ts/deliberation-helpers.ts and their specs — real but bounded blast radius, gives typed/parseable provenance), or (B) leave groundedBy null and set only the free-text groundingProvenance field (zero type changes, but provenance is unparseable for downstream consumers)? Confirmed the existing degradedNoLlm field cannot be reused to prove llmCalled===false — it means the opposite (LLM-unavailable SHRUG fallback at deliberation.service.ts:905-943), so a new success-path signal is needed and its shape depends on Fork 1's answer.
