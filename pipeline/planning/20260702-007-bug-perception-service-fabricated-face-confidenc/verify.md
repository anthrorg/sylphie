# Opus plan-verify verdict — 20260702-007

verdict: **needs-replan**

## Summary
Verified all four load-bearing claims line-by-line against live source at the cited locations — every one holds exactly, and the planner's two source corrections (dead-config is dormant-not-live since the hardcoded literals equal TrackingConfig defaults; the color loop is layer2_perception/feature_extraction.py, not the non-existent layer1_sensory path source.md names) are both accurate. Completeness is clean: all four source concerns map to tickets -a/-b/-c/-d and the layer3 non-goal is respected. Testability is strong: every AC is Given/When/Then with a concrete runnable check (unit tests for -b/-c, unit+integration for -a, before/after benchmark for -d). Tickets -b, -c, -d are atomic, single-surface, CANON-clean, and I would bet a build on them as written today — they are ready-for-queue. The item routes needs-replan ONLY because ticket -a carries a genuine, confirmed design fork with a CANON Std-4 theater angle: the MediaPipe FaceLandmarker Tasks API the code actually calls exposes no per-face detection-confidence field (its result has only face_landmarks/face_blendshapes/facial_transformation_matrixes), so -a cannot be built as source.md literally proposes and needs an architect ruling first. I strongly endorse the planner's split_recommendation: advance -b/-c/-d to refine/queue now under the new epic and hold -a in replan pending the architect ruling — do not let -a block the three clean fixes. This split is process routing under contract_write=autonomous, not new application direction, so it does not need Jim.

## Critical gaps
- None that block -b/-c/-d — those three are build-ready as written.
- Ticket -a is the only blocker and is correctly held for replan (design fork below).
- Minor/non-blocking: source.md's Evidence section (line 27) still cites the wrong color-loop path 'feature_extraction.py:154-166' under an implied layer1 root; the tickets use the correct layer2_perception path, so builders are unaffected — cosmetic only.
- Minor/non-blocking: ticket -d offers 'throttle OR vectorize' as either-satisfies. This is a low-risk implementation choice (not an architectural fork), fine to leave to marr at build time, but the builder must pick one and the carry-forward-color AC must hold for whichever is chosen.
- Builder awareness for -b (not a gap, a correctness note): the tracker state machine cannot distinguish a TENTATIVE-origin LOST from a CONFIRMED-origin LOST by state alone, so the fix MUST key off accumulated frames_seen>=min_confirm_frames — which -b's two ACs already encode correctly.

## Architect questions
- Q-face-confidence-signal (blocks ticket -a only): MediaPipe's FaceLandmarker Tasks API — confirmed as the API face_detector.py actually calls — returns no per-face detection-confidence score (FaceLandmarkerResult exposes only face_landmarks/face_blendshapes/facial_transformation_matrixes; confidence_threshold is consumed only as an input filter, never handed back). So source.md's proposed fix ('derive confidence from the first blendshape category or MediaPipe's detection confidence') cannot be built — neither signal exists as a detection confidence. Which real option should -a implement: (a) run MediaPipe's separate FaceDetector task alongside/instead of the Landmarker for a genuine category.score, accepting a second CV model on the same /detect hot path that ticket -d is trying to de-load; (b) derive a documented geometry-based quality proxy (landmark completeness/stability) and label it a proxy, not a probability; or (c) redesign the face-snapshot.service.ts consumer gate to stop requiring a continuous confidence float (gate on landmark completeness, already partially done at :317). This crosses the perception-service / apps-sylphie package boundary and has a CANON Std-4 theater-prohibition angle — repo precedent DEC-32 rejected a structurally identical 'move the bar on a still-fabricated confidence' move, so options (a) and (b) must be judged against that.
- Sub-question for the same ruling: does the chosen option require changing the semantics of face-snapshot.service.ts's MIN_CONFIDENCE=0.65 gate, and is there any interaction with the CANON Std-3 0.60 confidence ceiling? (My read: MIN_CONFIDENCE is a crop-quality gate, not a guardian-gated identity/belief confidence, so the 0.60 ceiling does not bind here — but please confirm, since -a's resolution may reshape that threshold's meaning.)
