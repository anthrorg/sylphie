# Opus plan-verify verdict — 20260702-016

verdict: **needs-rework**

## Summary
Strong, unusually well-verified plan: I independently re-checked every load-bearing claim against source and all held (cycle.py save_checkpoint never persists _ewc; events.id is UUID so MAX(timestamp) watermark is correct; CycleGuardService has no quiesce API; EWCRegularizer Fisher is in-memory; docker-compose has exactly the 5 in-scope stores; observability spec §1 exists). CANON posture is a strength, not a risk — drive isolation is explicitly respected via push/ack (no RPC/pull), theater prohibition is enforced as fail-loud in -b/-c, and -d explicitly re-checks the TK-154 RLS/guardian lockdown survives restore. migration.md correctly classifies this as additive tooling. Three fixable gaps keep it from a clean queue rather than a replan. (1) Source acceptance criterion #4 — "two consecutive snapshots diffed yields a valid trend point, events_this_period == watermark delta" — has NO runnable check in any ticket; -c only tests single-snapshot shape/counts. (2) Ticket -a's quiesce() AC only blocks enqueue/drainNext, but runSelfTick (the 200ms self-tick path) sets tickInFlight itself and bypasses the queue — a self-tick could start after quiesce() unless the AC also gates it. (3) The plan bakes a NEW drive-engine quiesce push/ack handshake into -a's hard AC, but the source only asked to quiesce the decision cycle; whether coordinate-pausing the isolated drive process is actually required (vs. an MVCC-consistent pg_dump of drive-state being sufficient) is a genuine design fork on the root ticket and should get an architect ruling before -a is built. All three are targeted planner/architect fixes, not a full replan; the other four tickets and the core mechanism are queue-ready.

## Critical gaps
- Source acceptance criterion #4 (two-snapshot diff yields a valid metrics trend point; events_this_period matches the watermark delta) has no runnable acceptance check in any ticket — ticket -c tests single-snapshot shape and exact counts but never the diff-of-two-snapshots trend computation. A stated feature AC for a P0 production item is untested; add a runnable check (likely in -c or a new cross-snapshot assertion).
- Ticket -a quiesce() acceptance criteria only block enqueue/drainNext admission, but CycleGuardService.runSelfTick() (the 200ms background self-tick) sets tickInFlight independently and bypasses the queue drain path — a self-initiated cycle could start after quiesce() is called. The AC must also assert that no self-tick is admitted during quiesce, or the snapshot can race a live cycle write.
- tensor_arch_version placement is ambiguous: observability spec §1 puts both schema_version and tensor_arch_version at the top level of the SAME manifest.json, but the plan writes schema_version in -a's manifest.json and tensor_arch_version only into -b's separate tensor_manifest.json. Reconcile so the manifest schema-shape test in -c can assert the spec's top-level keys exist.

## Architect questions
- Does a coordinated Sylphie Snapshot actually require coordinate-quiescing the isolated drive-engine process (the new SNAPSHOT_QUIESCE_REQUEST -> SNAPSHOT_QUIESCED push/ack handshake ticket -a bakes into its hard AC), or is an MVCC-consistent pg_dump of the drive-state Postgres store sufficient for intra-store consistency, with the TimescaleDB MAX(timestamp) watermark handling cross-store consistency? The source only asked to quiesce the decision cycle; the drive-engine handshake is planner-added scope on the root ticket. An inbound WS channel into drive-server exists so it is buildable, but building an unnecessary cross-process handshake (or omitting a necessary one) both change ticket -a's core design — needs a ruling before -a is built.
