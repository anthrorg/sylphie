- `2026-06-25T19:16:34+00:00` **ingested** from `inbox/sylphie-interaction-directive.md` -> planning/ (type guess: bug)
- `2026-06-25T19:25:36+00:00` moved **planning -> replan** — needs /explore-topic reconciliation: proactive-vs-turn-taking design tension; no atomic AC until reconciled
- `2026-06-25T19:43:56+00:00` moved **replan -> planning** — resolved: parking tension (proactive-vs-turn-taking) already reconciled in-contract by EP-20/TK-97..105 + DEC-26 (Jim-approved); see DEC-27. Re-entering planning to ATTACH to EP-20 (dedupe), not create a new epic.
- `2026-06-26T13:09:29+00:00` moved **planning -> refine** — ATTACH to EP-20 / TK-97..105 (dedupe per DEC-27); +TK-106 staged (demo perception kill-switch, the one uncovered directive point); dbcheck keyword-only false-positive, migration.md no-op filed for sentinel
- `2026-06-26T17:09:44+00:00` moved **refine -> queue** — atomic, red-teamed clean
- `2026-06-27T04:10:00+00:00` **pipeline-execute DRY RUN** (execute_mode=`plan-only`) — no build, item NOT moved. Findings:
  - **Readiness check could not run against the live contract.** `python .claude/workflows/pick_ready.py` crashed: `planning/contract.yaml` (working tree) is CORRUPT — truncated mid-changelog at the v2.0 entry (unterminated quoted scalar, no EOF newline). `git diff` shows the working copy dropped 4 append-only changelog entries (rest of v2.0, plus v2.1/v2.2/v2.3) vs HEAD. **HEAD-committed `contract.yaml` is intact and parses clean.** This is an uncommitted, interrupted/botched write to the source-of-truth and is a hard blocker for any real `to-pr` run. Recommended remedy (NOT applied here — out of this cog's plan-only remit): restore from the sound commit (`git checkout planning/contract.yaml`, HEAD `e8affbf`) since HEAD parses and contains the full v2.0–v2.3 history. Not mutating the contract autonomously.
  - **Readiness, evaluated read-only against HEAD-committed contract:** EP-20 / TK-97..105 are **all `done`**. This item attaches to those (already landed) — nothing buildable remains there.
  - **What WOULD build for this item: nothing pickable right now.** The only NEW buildable delta this item carries is the staged **TK-106** (demo perception kill-switch — runtime `PERCEPTION_ENABLED` flag in `apps/sylphie/src/gateways/perception.gateway.ts`, gating frame ingestion + the WS5 scene-change nudge; no DB, no UI). Per `contract_write=staged`, **TK-106 is NOT a contract node** — it lives only in this item's `plan.md` awaiting the explicit approval gate. `pick_ready.py` only picks contract nodes, so TK-106 cannot be selected and `item.json:contract_nodes` (EP-20, TK-97..105) are all done. ⇒ In a real `to-pr` run this item is a **no-op/blocked-on-approval**, not a build: TK-106 must be written into `contract.yaml` (approval gate) before execute can pick it.
  - (For reference, `pick_ready` against HEAD surfaces TK-94 + TK-95 (EP-19, P1, telemetry/DB connectivity) as the only ready `todo` tickets repo-wide — those belong to a different intake item, not 20260625-003.)
  - **No state move, no build, no contract write, no DB touch.** Dry run complete.
- `2026-07-02T02:29:13-04:00` pipeline-execute (to-pr): nothing to build — all contract_nodes (EP-20, TK-97..TK-105) are done, and the previously-staged TK-106 is now IN the contract and also done (landed since the 06-27 dry run; contract parses clean, prior corruption resolved). Item's entire scope is complete in the contract; no PR to open, so no queue->working->review move per runbook (review is gated on a PR). Left in queue for sweep/Jim to close out — suggest verifying TK-106 ACs and retiring this item. No build, no move.
- `2026-07-02T02:38:41-04:00` **FORCED** moved **queue -> done** — No legal queue exit: scope fully landed in contract without a pipeline build (EP-20/TK-97..105 done per DEC-27 attach; TK-106 verified — health.controller.ts cheap GET /api/health, Dockerfile HEALTHCHECK repointed, live 200 in changelog v2.4/v2.6). Jim-directed close-out 2026-07-02.
