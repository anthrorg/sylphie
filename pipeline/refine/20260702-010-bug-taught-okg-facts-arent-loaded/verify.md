# Opus plan-verify verdict — 20260702-010

verdict: **ready-for-queue**

## Summary
I independently verified every load-bearing claim against current main and the plan holds. Root cause is accurate: on a cold PersonModelService.cache, the WS connect path (ensurePersonNode, conversation.gateway.ts:296) only MERGEs the anchor and never calls loadFacts, so the per-turn person_model slot (communication.service.ts:419-422) stays null until re-teaching or the "Who am I?" trigger (the sole loadFacts caller, :659). I confirmed the crux the whole bug hinges on: detectTriggerPhrase (:1517-1521) matches only "^who am i$"/"what do you know about me"/"tell me what you know about me", so the repro's "what's my name?" genuinely falls through to the normal intakeTurn pipeline and needs the slot populated. Completeness: every source concern maps to a ticket; the two tickets (010-a primitives + unit tests, 010-b connect-wiring + regression) are atomic, correctly dependency-chained, and need no split. CANON-clean: pure read-timing/ordering around the existing read-only Cypher, touching no drive/provenance/confidence-ceiling/theater/guardian-tiering logic (deriveOkgFactTier explicitly untouched); DB gate n/a is correctly evidenced. ACs are runnable (tsx specs with mocked Neo4j sessions, controlled/delayed and rejecting mocks to prove race-closure and outage-safety by ordering, not "fast enough"). Two minor non-blocking notes for the builder, not gaps: (1) awaiting awaitHydration before intakeTurn requires making the existing .then((handled) => {...}) continuation async — this does not change intakeTurn's signature and is correctly derivable; (2) source AC#1's end-to-end knowledgeGrounding === 'GROUNDED' assertion is carried as the live-verify note rather than an automated check, which is faithful to source.md's own AC split (AC#1 live GROUNDED, AC#2 unit-level slot) and honors the repo's "verify before presenting" rule. Epic id EP-28 is already flagged to re-confirm free at queue time. No design fork, no architect/Jim ruling needed.

