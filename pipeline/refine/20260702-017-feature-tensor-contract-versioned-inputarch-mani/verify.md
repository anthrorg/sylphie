# Opus plan-verify verdict — 20260702-017

verdict: **ready-for-queue**

## Summary
Every load-bearing claim in the plan checks out against the actual code — I verified all five: GlobalModel.load()'s silent Xavier fallback (global_model.py:316-321, docstring confirms it), EWCRegularizer's total lack of persistence (replay.py, no save/load), the 1561-float literal layout in _assemble_global_input (cycle.py:176-198), save_checkpoint's real hook point (cycle.py:200), and the alphabetical ModalityRegistryService.getAll() sort (:28-32) with sensory-fusion.ts deriving offsets from it. The plan's correction of source.md's wrong scope hint (registry lives in packages/decision-making owned by cortex/luria, not packages/shared/forge) is factually correct. COMPLETENESS: every source.md concern and all four source ACs map to a ticket (three-axis manifest→-b, fail-loud refusal→-c, fixed-slot registry→-a, expand/Fisher-reanchor/forced-audit→-e, Fisher binding→-d); retrain-from-experience is correctly deferred to sibling item 20260702-014 per source's own non-goal. TESTABILITY: every AC names a concrete pytest/jest function with a run command — no "works correctly" vagueness. CANON: clean, and -c/-e actively reinforce theater-prohibition and the confidence ceiling (expand forces bootstrap mode back to audit); no DB surface (files only), migration.md as deliberate n/a is correct. No design fork was guessed at — the continue/expand/retrain design comes straight from the authored persistence-migration-plan doc, so no architect ruling is owed. Only -a (self-contained TS) is buildable immediately; -b..-e correctly depend_on the EP-23/TK-119..122 sidecar PR (still mid-build in pipeline/working) merging first, which is normal choreography, not a plan gap.

## Critical gaps
- Non-blocking builder note (fold in at refine, not rework): -c AC2's 'live structural fingerprint of the macro-slot field-name order' presumes a machine-readable declaration of the 5-slot layout, but today that order is only an implicit code order + docstring in _assemble_global_input (cycle.py:176-198). To make the fingerprint drift-check mechanical, the builder must introduce an explicit declarative MACRO_SLOT_LAYOUT (name+dim list) as the source of truth and rebuild _assemble_global_input from it. This is implied by -b/-c's ACs and is small, but it is an unstated sub-deliverable the builder should not overlook.
- Non-blocking note: -e AC3 forces bootstrap mode back to audit/shadow while -e's non-goals say 'no change to bootstrap ladder thresholds.' These are consistent only if the builder invokes the EXISTING demotion mechanism in inference/bootstrap.py rather than introducing a new threshold — worth stating so the two don't collide.
- Minor coverage note: -c tests the refusal, legacy-no-manifest, and non-swallow paths but has no explicit positive test that a MATCHING manifest loads and keeps training (the 'continue' strategy happy path). -d AC3 covers it indirectly (matching Fisher loads + arms EWC, which requires version match), so this is a soft gap, not a hole.

