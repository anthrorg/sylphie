# Migration plan — 20260702-017

**Touches a database / schema surface?** No. n/a.

## Why this is genuinely n/a (not a keyword false-positive left unexamined)

Verified directly against source, per `pipeline/policies/db-change-safety.md`'s definition
of "touches a DB" (any Postgres/Timescale/Neo4j surface, any `*.sql`/`*.cypher`, any
`vector(N)` dimension or embedding-dim/version constant used as a DB column type):

- All five tickets in this item's plan (`20260702-017-a` through `-e`) read/write only:
  - `packages/cognition-service/config.WEIGHTS_DIR` / `config.FOUNDATION_DIR` — plain
    directories on local disk holding `.npz` / `.weights.h5` checkpoint files.
  - A new `tensor_manifest.json` file and a new `fisher/*.npz` file, both plain files
    alongside the existing checkpoint files — not a database row/table.
  - In-process TypeScript constants (`FUSION_SLOT_ORDER`, `FUSION_REGISTRY_VERSION`) in
    `packages/decision-making` — not persisted anywhere, recomputed at process start.
- No file in scope touches `infra/**`, `prisma/migrations/**`, `infra/migrations/**`, any
  `*.sql`/`*.cypher`, or any of the three Neo4j stores, Postgres, or TimescaleDB.
- `EMBEDDING_VERSION` (`packages/shared/src/types/sensory-frame.ts`) is referenced
  read-only by ticket `-b` to compose `input_contract_version` — this ticket does not
  bump or redefine `EMBEDDING_VERSION` itself, and `EMBEDDING_VERSION` is not a DB column
  type anywhere (it participates in an in-memory fingerprint hash preimage, not a stored
  `vector(N)` dimension).
- The word "migration" throughout source.md ("migration strategies", "migration
  framework") refers exclusively to the **tensor's own checkpoint-loading strategy**
  (continue / expand / retrain-from-experience) — an ML/weights concept — never a
  database schema migration. This is the exact keyword false-positive the pipeline's
  tracked gotcha list already calls out ("Tighten `dbcheck`: prose mentions of
  'migration'/'postgres' false-positive").

**No migration-plan.template.md sections apply.** No forward migration, backfill, backup/
reverse, or continuity proof is needed because nothing durable/shared-state is being
altered — only local model-checkpoint files that are already routinely
overwritten/regenerated by training, and a new sibling file (`tensor_manifest.json`) that
starts absent everywhere and is additively introduced (ticket `-c`, AC3: absence is
handled as `"unstamped-legacy"`, never a hard failure).
