{
  "$comment": "Knobs for the intake pipeline. Edit these; pipeline.py and the RUNBOOK read them.",
  "states": [
    "inbox",
    "planning",
    "refine",
    "queue",
    "working",
    "review",
    "refactor",
    "replan",
    "done",
    "archive"
  ],
  "max_items_per_tick": 3,
  "execute_mode": "to-pr",
  "$execute_mode": "to-pr: execute cog runs /worktree-agents to an OPEN PR (never merges). plan-only: dry-run only.",
  "contract_routing": "EP-21",
  "$contract_routing": "EP-21 = the rolling intake epic (renamed from EP-INTAKE 2026-07-02 so ids satisfy the planning-worx schema /^(FEAT|EP|TK|TASK)-\\d+$/). Intake tickets nest under EP-21 (parent FEAT-3); larger items that warrant their own epic get the next free numeric EP-## assigned at contract-write time, not a semantic name.",
  "contract_write": "staged",
  "$contract_write": "staged = ticket/epic writes into contract.yaml are GATED on Jim approval. Governance appends (decisions + changelog, append-only) ARE allowed autonomously as routing/record. A cog must NOT write contract.yaml while another writer has it dirty (concurrency guard).",
  "max_replan_attempts": 2,
  "max_refactor_attempts": 2,
  "stuck_threshold_hours": 48,
  "archive_after_days": 14,
  "contract_path": "planning/contract.yaml",
  "id_prefix_format": "%Y%m%d",
  "default_priority": "P2"
}
