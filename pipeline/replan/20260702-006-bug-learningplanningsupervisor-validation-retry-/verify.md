# Opus plan-verify verdict — 20260702-006

verdict: **needs-replan**

## Summary
I verified all 5 source findings line-for-line against HEAD 13a33e8 — every code claim is accurate (no drift from the 228df73 source-trace), and completeness is good: all 4 required-Acceptance findings are ticketed, the 8 "Lower" defects are correctly deferred (they have no acceptance criteria), and OQ-1/backfill is correctly routed to governance rather than guessed. Tickets -a (validation-retry proposal) and -b (carry-forward predictedDriveEffects) are clean, atomic, testable, and queue-ready; ticket -c's PROVENANCE half (criterion 1, GUARDIAN→CANDIDATE for self_reported speaker triples) is also ready and can ship independently. Two problems block a build as-written. (1) Ticket -c criterion 2 (subject resolution) contains an unsurfaced design fork the planner mischaracterized as "extending an already-established MERGE-by-grounding_person_id pattern" — no such pattern exists: upsertValueEntity MERGEs :Candidate by {label} and stamps grounding_person_id only as a property, and post-Wave-3 no speaker/subject node is minted at all, so a per-speaker subject node has to be invented (its label, merge key, and reconciliation with third-person mentions and the person-model are a WKG identity-modeling call for architect/atlas). (2) Ticket -d requires distinguishing a duplicate reject (keep has_planned=true) from a rate-limit/cap reject (leave unmarked), but enqueue() returns a bare boolean and -d's files_in_scope wrongly declares opportunity-queue.service.ts "unchanged." No CANON violation is introduced (the fixes restore provenance/ceiling/theater compliance) and migration.md's N/A verdict is correct — no schema/destructive change. Route: split -c into -c1 (provenance, queue-ready) and -c2 (subject-node model, blocked on the ruling); fix -d's enqueue contract; then a/b/c1/d go to queue.</summary>
<parameter name="critical_gaps">["Ticket -c criterion 2 mischaracterizes a design fork as a mechanical extension: the claimed 'MERGE-by-grounding_person_id pattern already established in this file' does not exist — upsertValueEntity (extract-typed-edges.service.ts:541-576) MERGEs :Candidate by {label} and writes grounding_person_id only as a coalesced property; no speaker/subject node is minted anywhere post-Wave-3. Split -c into -c1 (provenance fix, queue-ready) and -c2 (subject resolution, blocked on the architect ruling below).", "Ticket -d cannot be built as scoped: enqueue() (opportunity-queue.service.ts:87-136) returns a bare boolean collapsing duplicate/rate-limit/cap rejects, but -d must keep duplicates marked has_planned=true (else a re-poll mints a second plan for the same contextFingerprint — the fragmentation dedup exists to prevent) while leaving rate-limit/cap rejects unmarked. This requires enqueue() to return a discriminated reason (a small change to opportunity-queue.service.ts), directly contradicting -d's files_in_scope note that that file is 'unchanged, just consumed correctly.' Resolve so the builder doesn't ship a racy call-site re-derivation of the rejection reason.", "Non-blocking for this item, but flag to coordinator: one of the deferred 'Lower' defects (re-grounding sweep pushing INFERENCE confidence past the 0.60 ceiling) is a live CANON Std-3 confidence-ceiling violation being parked to a future item — acceptable since it's outside this item's required-Acceptance scope and has no GWT, but it should be triaged, not lost."]

## Architect questions
- Ticket -c subject resolution: how should a self-reported speaker's SUBJECT node be represented in the WORLD Neo4j graph? There is no existing per-speaker node to attach to (existing :Candidate value nodes MERGE by {label}, not by speaker identity, and nothing mints a speaker/person node post-Wave-3). A ruling is needed on: (a) the node label (reuse :Candidate vs a dedicated Person/Self node); (b) the stable merge key (grounding_person_id/speakerId); (c) how it reconciles with third-person mentions of the same person, which resolve by _subjectLabel (subjectHint!=='speaker'), so the same human could yield two disjoint nodes; and (d) whether/how it relates to the person-model identity. This is a WKG/Self-Other KG identity-modeling decision (architect, with atlas as the KG-modeling voice), not the mechanical pattern-extension the plan assumes.
