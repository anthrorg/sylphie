# Opus plan-verify verdict — 20260702-012

verdict: **needs-replan**

## Summary
I agree with the planner's replan verdict, and my adversarial pass strengthens it. Spot-checked the load-bearing claims against source: bootstrap.py:55 (0.85), tensor-candidate-builder.ts:42-46 (0.79/0.95 caps), rules.ts:212-215 (Guilt 0.15 on guardian_correction), and ACTION_TYPE_DEFAULTS-is-an-effects-table-not-a-selection-policy all verified EXACT — the plan's existing-vs-new scope split is accurate and it correctly trims scope rather than creeping. Completeness is good: every source concern maps to a ticket (a-i). BUT I found one material error the planner got wrong: verification item 6 / ticket 012-i claims detectDepressiveAttractor() 'composites any drive over 0.60 so Guilt already participates with no new code.' False — attractor-monitor.service.ts:489-491 hardcodes Signal 3 to Sadness and Anxiety only; grep confirms Guilt appears nowhere in the file, and the 3-signal averaging means Guilt-alone-high won't cross threshold even if added. So source Acceptance #5 is genuine NEW scope requiring a code change to the existing detector, not a no-code regression proof — ticket 012-i is misscoped and needs re-derivation. Combined with the two source-declared design-blockers (stakes(), veto), the verified undeclared 'what mechanically is the floor' gap, and three of five prerequisites still open (016/017 planning, 002 working, plus undeclared 015 boot-checksum), this cannot enter the build queue until architect rules. The a-i ticket set is otherwise well-formed with genuinely runnable checks (yarn/pytest/grep) and should convert to refine largely as-is post-ruling — with 012-i corrected.

## Critical gaps
- Ticket 012-i is misscoped on a false premise: it asserts sustained unresolved Guilt already trips detectDepressiveAttractor() with zero new code, but Guilt is not among the drives that detector reads (Signal 3 = max(Sadness, Anxiety) only; grep confirms no Guilt reference in attractor-monitor.service.ts). The AC would fail as written and the 'no new detector code' non-goal is unachievable — needs re-derivation into a real code change post-ruling.
- Three of five named prerequisites are still open (feature-snapshot-restore 016 and feature-tensor-contract 017 in planning; bug-audit-cognition-sidecar 002 in working — graduation cannot be built on the 422-ing sidecar), plus an undeclared dependency on the unbuilt boot-checksum in schema-versioning item 015. Cannot sequence into a build queue until these close, independent of the design forks.
- Two source-declared design-blockers (stakes() and the veto) plus one verified undeclared design gap (no action-selection 'floor' mechanism exists today in the assumed shape) mean tickets b-d cannot be written to a testable spec until architect defines them — the plan correctly gates b-i on the ruling but the ruling has not happened yet.

## Architect questions
- stakes(action, driveState) exact contract: signature, continuous score vs discrete tier enum, low/high boundary rule, and how it composes with the ALREADY-BUILT per-category ladder (0.85 agreement / 0.79 cap) — is stakes-tier a hard ceiling on max bootstrap mode a category may reach, or a separate multiplier on the existing thresholds?
- What mechanically IS 'the floor' the tensor-blind veto diffs against? Verified NO hardcoded action-SELECTION policy exists (ACTION_TYPE_DEFAULTS only computes drive effects for an already-chosen action's outcome; the only divergence mechanism at decision-making.service.ts:882 reads tensorResult.divergenceScore and is thus downstream of the tensor, not tensor-blind). Architect must specify the '(hardcoded rules, drive vector) -> would-have-picked-X' computation before a veto predicate can be written or property-tested.
- Boot-checksum scope: source ties veto immutability to a CANON Std-6 boot-checksum from the still-unbuilt schema-versioning feature (item 20260702-015, planning). Build a minimal standalone checksum now, or hard-block ticket c on 015 landing first?
- Rule-strength write path for per-action-class tolerance (Guilt resolution, ticket g): drive_rules is REVOKE+RLS locked with a guardian-privileged pool (TK-154/155, merged). Does the vindicated-loosen/punished-strengthen adjustment write through that existing pool unchanged, or need a new guardian-approved nullable column + migration? (Determines migration.md's final shape.)
- NEW (surfaced by my verification, not in the planner's list): The Depressive Attractor detector does NOT currently read Guilt — Signal 3 is hardcoded to Sadness/Anxiety and the composite averages 3 signals. To satisfy source Acceptance #5, does Guilt get added to the depressive-composite drive set, and if so how does it cross threshold given the all-three-signals-must-be-elevated averaging design? This is also a self-evaluation/CANON-adjacent call (modifying a monitoring detector) that must be ruled before ticket 012-i can be re-scoped from 'no-code regression proof' to a real, testable change.
