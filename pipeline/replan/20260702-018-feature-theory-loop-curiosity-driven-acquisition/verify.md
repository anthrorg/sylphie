# Opus plan-verify verdict — 20260702-018

verdict: **needs-replan**

## Summary
Concur with the planner's own route: needs-replan, and for the right reason. The plan is unusually rigorous — I spot-checked every load-bearing claim against source and all hold: the autonomous RESEARCH_ENTITY path is already built (decision-tick-engine boredom>0.6/curiosity>0.7 + pickResearchTarget confidence<0.60, no staleness), but its write path (action-handler-registry, under action-handlers/ not the services/ path the plan cites) stamps results as plain :Entity/INFERENCE@0.30 — the naive-hoarding failure the source warns about — and no :Theory shape exists. The acquisition slice (018-a/b/c) is genuinely atomic with runnable-check ACs anchored to real files (candidate-grounding-exclusion.spec.ts, no-theater.spec.ts, action-outcome-reporter.service.spec.ts all exist; CON-2 verified verbatim; migration.md is additive-only and DB-safety-clean). The confirmation slice (Epic B) correctly cannot be ticketed: provenance.types.ts codifies that the only path above the 0.60 ceiling is guardian-confirmation at 0.90 under Std-5, so an automated Tess_Confirmed tier is a real CANON fork, and Tess has zero prior art in the repo so its transport/payload contract is uninventable. The planner refused to guess either — correct. Nothing is queue-ready today regardless: even Epic A is sequence-blocked on four still-unticketed sibling items (008/006/015/016), and its depends_on point at pipeline item IDs, not contract TK-* IDs. Route to replan for an architect ruling on the two forks; hold Epic B as a governance open_question rather than re-entering planning on a timer.

## Critical gaps
- Epic A is not buildable yet: 018-a depends_on 20260702-008 (WKG write-path bug fix) which is itself unticketed in planning/; the theory writes ride exactly that plumbing. depends_on also reference pipeline item IDs, not contract node IDs — must resolve to TK-* before any contract write.
- 018-a AC#1 asserts a literal confidence: 0.35 that is a guessed magic number (existing web-research path uses 0.30). The theory node's confidence should derive from the provenance base-confidence table for LLM_GENERATED, not a hardcoded literal — fix or justify before build.
- 018-b AC#1 requires filtering research targets by staleness (hoursSinceLastActivity), but I could not confirm queryEntities('*') returns any timestamp/last_retrieval_at field. If it does not, 018-b silently expands to a WKG read-path change — unscoped. Verify before build.
- 018-b AC#1's staleness threshold is undefined (open question #3); the runnable check ('a stale-but-high-confidence entity IS eligible') cannot be written until the number is picked. Acceptable to defer to briefing-builder with a logged governance assumption at prototype level, but it must be logged, not guessed silently.
- 018-c spans two owning trios (drive-engine action-outcome-reporter for AC#1 = drive/skinner; decision-making no-theater.spec for AC#2 = cortex/luria) — coordinate ownership; not fatal but not a single-owner ticket.
- Path hygiene: plan cites action-handler-registry.service.ts under packages/decision-making/src/services/ but it lives under src/action-handlers/; and provenance.types.ts (the :Candidate-exclusion pattern 018-a mirrors) lives in packages/shared/src/types (forge-owned), not decision-making/wkg (atlas) as the scope hints imply.

## Architect questions
- CANON confidence-ceiling conflict (blocks all of Epic B): does the proposed Tess_Confirmed provenance tier stay within the existing <=0.60 non-guardian ceiling (promoted-but-bounded, decaying slowly — CANON-compliant, no exception), or is it meant to exceed 0.60 the way only guardian-confirmation (0.90, Std-5) currently can? provenance.types.ts explicitly reserves the above-ceiling path to a verified guardian. If the answer is 'exceed', it requires a new CANON exception via update-canon with Jim's explicit approval before any Epic-B code — this is not the planner's or coordinator's call to make.
- Tess invocation/transport contract (blocks Epic B tickets B1-B4): Tess has zero references anywhere in the codebase — no CLI, queue, or file-drop convention exists. How does Sylphie send a theory and receive a verdict — synchronous CLI subprocess, async file-drop (this pipeline's own inbox/outbox is a plausible precedent), or a queue? What are the literal request and verdict payload shapes? Source flags this as its own open question #3 naming architect as owner; no runnable-check AC can be written for the confirmation loop until this is placed.
